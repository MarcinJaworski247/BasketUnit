import Index from '../views/Index.vue';

const routes = [
        {
            path: `/dashboard`,
            name: `dashboard.index`,
            component: Index
        }
    ]

export default routes;

